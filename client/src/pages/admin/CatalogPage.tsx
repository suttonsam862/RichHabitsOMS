This change improves file tracking in the catalog form submission by checking both `selectedFile` property and `files` property of the input element, providing more reliable access to uploaded files.
```